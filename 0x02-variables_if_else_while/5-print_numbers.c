#include <stdio.h>

/**
*main - prints all single digit numbers in base 10
*Return: Always ) (Success)
*/
int main(void)
{
	int i =0;
	
	while (i < 10)
	{
		printf("%i", i);
		i++;
	}
	putchar('\n');

	return (0);
}
